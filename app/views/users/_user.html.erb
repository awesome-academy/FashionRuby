<li>
<%= link_to user.name, user,class: 'acount-btn'  %>
<% if current_user.admin? && !current_user?(user) %>
| <%= link_to t("global.canpaigs.delete"), user, method: :delete,
data: { confirm: "You sure?" },class: 'btn btn-success' %>
<br>
  <%= link_to t("global.header.adminmanager"),new_admin_canpaign_path,class: 'btn btn-success'%>
<% end %>
</li>

