<table class="table table-hover">
    <thead>
        <tr>
            <th scope="col">
                <%=t("global.canpaigs.name") %>
            </th>
        </tr>
    </thead>
    <tbody>
        <% @users.each do |user| %>
        <tr>
            <td>
                <%= user.name %>
            </td>
        </tr>
        <% end %>
    </tbody>
</table>
<%= link_to t("global.header.adminmanager"),new_admin_canpaign_path,class: 'btn btn-success'%>
<%= link_to t("global.header.admin1"),admin_products_path,class: 'btn btn-success'%>
<%= link_to t("global.header.adminmanager2"), admin_orders_path, class: 'btn btn-success' %>
