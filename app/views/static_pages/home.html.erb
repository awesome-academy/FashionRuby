<div class="clearfix"> </div>
</div>
<script>
// $(document).ready(function() {
//     $('.popup-with-zoom-anim').magnificPopup({
//         type: 'inline',
//         fixedContentPos: false,
//         fixedBgPos: true,
//         overflowY: 'auto',
//         closeBtnInside: true,
//         preloader: false,
//         midClick: true,
//         removalDelay: 300,
//         mainClass: 'my-mfp-zoom-in'
//     });

// });
</script>
</div>
</div>
</div>
<!--banner-->
<div class="banner">
    <div class="matter-banner">
        <div class="slider">
            <div class="callbacks_container">
                <ul class="rslides" id="slider">
                    <li>
                        <%= image_tag("2.jpg",  width: "200px") %>
                    </li>
                    <div class="tes animated wow fadeInUp animated" data-wow-delay=".5s" style="visibility: visible; animation-delay: 0.5s; animation-name: fadeInUp;">
                        <h2>
                            <%= t".title" %>
                        </h2>
                    </div>
            </div>
            </li>
            </ul>
        </div>
    </div>
</div>
<div class="clearfix"> </div>
</div>
<div class="content">
    <div class="container">
        <div class="content-top">
            <div class="content-top1">
                <div class="clearfix"> </div>
            </div>
            <h1 style="background-color:#ad4095">
                <%= t".onsale" %>
            </h1>
            <div class="content-top1">
                <% unless  @canpaign.nil? %>
                <%  @canpaign.products.each do |product|%>
                <div class="col-md-3 col-md2 animated wow fadeInLeft" data-wow-delay=".5s">
                    <div class="col-md1 simpleCart_shelfItem">
                        <% product.images.each do |image|%>
                        <%= image_tag image.variant(resize_to_limit: [100, 100]) %>
                        <% end %>
                        </a>
                        <h3>
                            <%= link_to product.name, product_path(product.id) %>
                        </h3>
                        <div class="price">


                             <h5 class="item_price">
                        <% if @sales_product_ids.include?(product.id) %>
                                <br><%=number_to_currency product.price*(100- @canpaign.value)/100 %>
                         <br><br> <span class="onsale">
                            <p>
                                <%= @canpaign.value  %>%    <br>  <%= t("global.canpaigs.discout") %></p> </span>
                                <del><%= number_to_currency product.price %></del>
                        <% else %>
                            <span> <%= number_to_currency product.price %> </span>
                        <%end%>

                            </h5>
                            <%= link_to t(".addcart"), cart_path(product),remote: true, class: 'btn btn-primary' %>
                            <div class="clearfix"> </div>
                        </div>
                    </div>
                </div>
                <% end %>
                <% end %>
            </div>
            <div class="clearfix"> </div>
            <h1 style="background-color:#ad4095">
                <%= t".bestseller" %>
            </h1>
            <div class="col-md1 simpleCart_shelfItem">
                <div class="content-top1">
                </div>
                <div class="clearfix"></div>
                <% @bestseller.each do |product|%>
                <div class="col-md-4 con-tp-lft animated wow fadeInLeft" data-wow-delay=".5s">
                    <div class="col-md1 simpleCart_shelfItem">
                        <div class="content-grid-effect slow-zoom vertical">
                            <% product.images.each do |image|%>
                            <%= image_tag image.variant(resize_to_limit: [100, 100]) %>
                            <% end %>
                            <div class="info-box">
                                <div class="info-content simpleCart_shelfItem">
                                    <div class="price">
                                    </div>
                                </div>
                            </div>
                        </div>
                        </a>
                        <h3>
                            <%= link_to product.name, product_path(product.id) %>
                        </h3>
                        <h5 class="item_price">

                        <% if @sales_product_ids.include?(product.id) %>
                                <br><%=number_to_currency product.price*(100- @canpaign.value)/100 %>
                          <span class="onsale">
                                 <p> <%= @canpaign.value  %>% <br> <%= t("global.canpaigs.discout") %></p> </span>
                                <del><%= number_to_currency product.price %></del>
                        <% else %>
                            <span> <%= number_to_currency product.price %> </span>
                        <%end%>
                            </h5>

                        <%= link_to t(".addcart"), cart_path(product),remote: true, class: 'btn btn-primary' %></a>
                    </div>
                </div>
                <%end%>
                <div class="clearfix"></div>
                <h1 style="background-color:#ad4095">
                    <%= t".new" %>
                </h1>
                <div class="c-btm">
                    <div class="content-top1">
                        <% @products.each do |product|%>
                        <div class="container">
                            <div class="col-md-3 col-md2 animated wow fadeInLeft" data-wow-delay=".5s">
                                <div class="col-md1 simpleCart_shelfItem">
                                    <% product.images.each do |image|%>
                                    <%= image_tag image.variant(resize_to_limit: [100, 500]) %>
                                    <% end %>
                                    </a>
                                    <h3>
                                        <%= link_to product.name, product_path(product.id) %>
                                    </h3>
                                    <div class="price">

                                         </h3>
                        <h5 class="item_price">
                        <% if @sales_product_ids.include?(product.id) %>
                                <br><%=number_to_currency product.price*(100- @canpaign.value)/100 %>
                                 <span class="onsale">
                                 <p> <%= @canpaign.value  %>% <br> <%= t("global.canpaigs.discout") %></p> </span>
                                <del><%= number_to_currency product.price %></del>
                        <% else %>
                            <span> <%= number_to_currency product.price %> </span>
                        <%end%>
                            </h5>

                                        <%= link_to t(".addcart"), cart_path(product),remote: true, class: 'btn btn-primary' %></a>
                                        <div class="clearfix"> </div>
                                    </div>
                                </div>
                            </div>
                            <% end %>
                        </div>
                        <div class="clearfix"> </div>
                    </div>
                </div>
            </div>
            <script type="text/javascript">
            $(document).ready(function() {
                $.ajax({
                    type: 'POST',
                    url: $(this).attr('action'),
                    data: $(this).serialize(),
                    // Mostramos un mensaje con la respuesta de PHP
                    success: function(data) {
                        $('#result').html();
                    }
                });
            });
            </script>
